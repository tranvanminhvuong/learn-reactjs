import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';
loading.propTypes = {

};

function loading(props) {
    return (
        <div>
            loading...
        </div>
    );
}

export default loading;